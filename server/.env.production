# Production Environment Variables for TimeBeacon Google Integration

# Google OAuth Configuration
GOOGLE_CLIENT_ID=696202687856-your-full-client-id-here
GOOGLE_CLIENT_SECRET=your-client-secret-here
GOOGLE_REDIRECT_URI=https://your-api-domain.com/auth/google/callback

# Server Configuration
NODE_ENV=production
PORT=3001

# Security
ENCRYPTION_KEY=generate-with-crypto-randomBytes-32-hex
FRONTEND_URL=https://app.timebeacon.io

# Generate encryption key with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"